[project]
name = "lm-studio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "lmstudio>=1.3.0",
    "pydantic-ai>=0.1.8",
]

[dependency-groups]
dev = [
    "ruff>=0.11.8",
]
